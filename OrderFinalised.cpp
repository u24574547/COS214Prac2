#include "OrderFinalised.h"
#include "Order.h"

void OrderFinalised::addPizza(Order* order, Pizza* pizza) {
    // Order is finalized, can't add
}

void OrderFinalised::removePizza(Order* order, int pizzaNo) {
    // Order is finalized, can't remove
}

void OrderFinalised::applyDiscount(Order* order, int discountType) {
    // Order is finalized, can't apply discount
}